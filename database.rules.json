{
  "rules": {
    "orders": {
      ".read": "auth.uid == 'REPLACE_WITH_ADMIN_UID'",
      "$orderKey": {
        ".read": true,
        ".write": "auth.uid == 'REPLACE_WITH_ADMIN_UID' || newData.exists()",
        ".validate": "auth.uid == 'REPLACE_WITH_ADMIN_UID' || (newData.hasChildren(['name', 'phone3', 'total', 'amount']) && newData.child('name').isString() && newData.child('name').val().length <= 50 && newData.child('phone3').isString() && newData.child('phone3').val().matches(/^[0-9]{3}$/) && ((newData.child('isPaid').val() == true) == (data.child('isPaid').val() == true)))"
      }
    },
    "settings": {
      ".read": true,
      ".write": "auth.uid == 'REPLACE_WITH_ADMIN_UID'"
    },
    "archives": {
      ".read": "auth.uid == 'REPLACE_WITH_ADMIN_UID'",
      "$archiveId": {
        ".write": "auth.uid == 'REPLACE_WITH_ADMIN_UID' && !data.exists()"
      }
    }
  }
}
